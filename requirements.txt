# Core dependencies for the Whisper + pyannote diarization application.
# The project previously relied on a separate requirements-local.txt file; the
# dependencies are now consolidated here for easier installation.
streamlit==1.39.0
faster-whisper==1.0.3
# PyTorch 2.6 provides official wheels for Python 3.13.  Keeping torch and
# torchaudio aligned avoids ABI mismatches during installation.
torch==2.6.0
torchaudio==2.6.0
librosa==0.10.2.post1
numpy==1.26.4
soundfile==0.12.1
ffmpeg-python==0.2.0
huggingface_hub==0.23.0
pyannote.audio==3.1.1
