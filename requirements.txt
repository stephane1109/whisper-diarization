# All runtime dependencies are already provided in the execution environment used by this project.
# Keeping this file intentionally empty prevents the platform from attempting to download the
# packages via pip, which fails because outbound internet access is blocked (ProxyError 403).
#
# For local development, install the dependencies listed in requirements-local.txt instead.
